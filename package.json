{
  "name": "slack-notion-preview",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "ts-node-dev --poll --respawn --transpile-only --files ./src/index.ts",
    "build": "tsc -p .",
    "start": "NODE_ENV=production node dist/src/index.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.2",
    "@slack/bolt": "^3.12.2",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "prettier": "^2.8.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.3"
  },
  "engines": {
    "node": "14.19.1"
  }
}
